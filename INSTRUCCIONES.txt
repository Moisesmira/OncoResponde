ONCORESPONDE — COMMIT 003 INCREMENTAL

Este paquete NO incluye package.json, package-lock.json, netlify.toml ni configuración de Vite.
Por tanto, no modifica las dependencias ni la instalación que ya funciona en Netlify.

Sustituye o añade únicamente estos archivos, respetando sus rutas:

1. src/App.tsx                         (sustituir)
2. src/styles.css                      (sustituir)
3. src/pages/WellnessHub.tsx           (añadir)
4. src/pages/WellnessTopic.tsx         (añadir)
5. src/pages/Breathing.tsx             (añadir)

No borres ni sustituyas Today.tsx, CancerProfile.tsx, package.json o package-lock.json.

Mensaje recomendado del commit:
Commit 003 - Más allá del tratamiento y respiración (incremental)
