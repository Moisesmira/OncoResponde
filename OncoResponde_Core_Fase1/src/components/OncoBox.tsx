import { useState } from 'react';
import { useNavigate } from 'react-router-dom';
export default function OncoBox({context='general'}:{context?:string}){const[q,setQ]=useState('');const nav=useNavigate();return <section className="oncobox"><h2>¿En qué puedo ayudarte?</h2><p className="muted">No incluyas datos que permitan identificarte.</p><textarea value={q} onChange={e=>setQ(e.target.value)} placeholder="Escribe tu pregunta…"/><div className="row"><button className="secondary" onClick={()=>{const SR=(window as any).SpeechRecognition||(window as any).webkitSpeechRecognition;if(!SR)return alert('El dictado no está disponible en este navegador.');const r=new SR();r.lang='es-ES';r.onresult=(e:any)=>setQ(e.results[0][0].transcript);r.start();}}>🎤 Dictar</button><button disabled={!q.trim()} onClick={()=>nav('/respuesta',{state:{question:q,context}})}>Preguntar</button></div></section>}
