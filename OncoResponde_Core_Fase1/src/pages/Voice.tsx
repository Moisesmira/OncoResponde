import {useState} from 'react';import NavHeader from '../components/NavHeader';import {useNavigate} from 'react-router-dom';
export default function Voice(){const[text,setText]=useState('');const nav=useNavigate();const listen=()=>{const SR=(window as any).SpeechRecognition||(window as any).webkitSpeechRecognition;if(!SR)return alert('El reconocimiento de voz no está disponible.');const r=new SR();r.lang='es-ES';r.onresult=(e:any)=>setText(e.results[0][0].transcript);r.start()};return <main><NavHeader title="Háblame"/><section className="card centered"><img className="voice-image" src="/assets/camino.png"/><h2>Puedes hablar con tranquilidad</h2><p>Tómate el tiempo que necesites. La conversación no se guarda automáticamente.</p><button className="mic" onClick={listen}>🎤 Comenzar a hablar</button><textarea value={text} onChange={e=>setText(e.target.value)} placeholder="También puedes escribir…"/><button disabled={!text.trim()} onClick={()=>nav('/respuesta',{state:{question:text,context:'conversación por voz'}})}>Enviar</button></section></main>}
